import { Box } from "@mui/material";
import React from "react";

const Projects = () => {
  return (
    <Box
      sx={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      Hello there
    </Box>
  );
};

export default Projects;
